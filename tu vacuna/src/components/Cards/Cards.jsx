import React from "react";
import './Cards.css'

function Cards ({text, variant, onClick}) {
    return(
        <div
        className={"variant"}
        onClick={onClick}
        >
        </div> 
    )
}